# YOLOv8 Object Detection API – FastAPI + Docker

- Typed JSON I/O, Prometheus `/metrics`, health `/healthz`
- CPU-only Torch; multi-stage Dockerfile; pytest tests

## Quickstart
```bash
cp .env.example .env
docker compose up --build

